# МОСТОВОЙ OS — Dashboard Implementation Plan

> План разработки модуля Dashboard (Split Focus Premium).
> Основа: `06_UI_Design_System.md` · `07_Pages_Specification.md` · `08_Database_Schema.md` · `09_Development_Rules.md`
> Статус: Ожидает подтверждения. Код не написан.

---

## Текущее состояние проекта

### Что уже есть

| Файл | Состояние |
|------|-----------|
| `src/app/dashboard/layout.tsx` | Существует. Sidebar + ClientLayout. Нужна доработка. |
| `src/app/dashboard/page.tsx` | Существует. **Старый дизайн** (фиолетовый). Полная замена. |
| `src/components/Sidebar.tsx` | Существует. Цвет `gray-950`, старые роли. Нужна замена. |
| `src/contexts/AuthContext.tsx` | Существует. Читает из `profiles`. Работает, трогать осторожно. |
| `src/types/index.ts` | Существует. Старые типы. Нужно расширить. |
| `src/lib/supabase.ts` | Существует. Browser client. |
| `src/lib/supabase-server.ts` | Существует. Server client. |

### Что отсутствует

- Topbar компонент
- `src/lib/supabase/admin.ts` (service client)
- `src/actions/` директория (Server Actions)
- `src/types/entities.ts` (бизнес-типы по схеме)
- Все Dashboard-специфичные компоненты (`LeftPanel`, `RightPanel`, etc.)
- Skeleton компоненты

### Ключевые расхождения с архитектурой

| Проблема | Текущий код | Целевой стандарт |
|---------|------------|-----------------|
| Таблица финансов | `finance_records` | `finance_transactions` |
| Таблица пользователей | `profiles` | `employees` |
| Ролевая система | `owner/manager/employee` | `owner/rop/mp/lmai/accountant` |
| Статусы сотрудников | `is_active: boolean` | `status: 'active'/'archived'` |
| Цвет sidebar | `gray-950` | `#0c1f33` (brand dark navy) |
| Dashboard дизайн | Фиолетовый, KPI cards | Split Focus: Left Month / Right Today |

---

## Принципы разбивки задач

- Каждая задача: **1–2 часа** работы
- Каждая задача завершена и работает независимо
- Порядок: Foundation → Layout → Data → Components → Polish
- Зависимости явно указаны — нельзя начинать задачу пока не завершены её зависимости

---

## Фаза 0: Foundation (подготовка)

### Задача 0.1 — Ревизия и документирование таблиц в реальной БД

**Что будет сделано:**
Проверить через Supabase Management API какие таблицы реально существуют в БД.
Сравнить с `08_Database_Schema.md`. Составить список расхождений.
Определить какие таблицы нужно создать / переименовать для Dashboard.

**Какие файлы изменяются:**
- Только чтение. Никаких изменений файлов.
- (Результаты фиксируются в этом документе в разделе "Реальное состояние БД")

**Supabase таблицы:**
- Чтение `information_schema.tables`
- Чтение `information_schema.columns`

**Зависимости:** нет

**Критерии готовности:**
- [ ] Список реальных таблиц получен
- [ ] Расхождения с 08_Database_Schema.md зафиксированы
- [ ] Понятно какие поля есть в `consultations`, `employees`, `profiles`
- [ ] Принято решение: мигрировать или адаптировать запросы к текущей схеме

---

### Задача 0.2 — Обновление TypeScript типов

**Что будет сделано:**
Обновить `src/types/index.ts` — привести типы в соответствие с реальной БД (после 0.1).
Добавить все типы необходимые для Dashboard:
- `Employee` с реальными полями
- `Consultation` с правильными статусами
- `DashboardMonthStats` — агрегат за месяц
- `DashboardTodayStats` — данные за сегодня
- `LiveFeedItem` — запись для живой ленты
- `TeamMemberStatus` — статус члена команды

**Какие файлы изменяются:**
- `src/types/index.ts` — обновление и расширение

**Supabase таблицы:**
- Нет обращений к БД. Только типизация.

**Зависимости:** Задача 0.1 (нужно знать реальную схему)

**Критерии готовности:**
- [ ] `tsc --noEmit` проходит без ошибок
- [ ] Все старые типы сохранены (обратная совместимость с существующими страницами)
- [ ] Новые типы экспортируются из `src/types/index.ts`
- [ ] Нет `any` типов в новых определениях

---

### Задача 0.3 — Утилиты и форматтеры

**Что будет сделано:**
Создать `src/lib/formatters.ts` с функциями форматирования для Dashboard:
- `formatMoney(amount: number): string` → "24 500 KGS"
- `formatPercent(value: number): string` → "36.4%"
- `formatDate(date: string): string` → "21 июн"
- `formatDateFull(date: string): string` → "21 июня 2025"
- `formatTime(time: string): string` → "14:30"
- `formatPhone(phone: string): string` → "+996 XXX XXX-XX-XX"
- `getInitials(name: string): string` → "СМ"
- `getDayGreeting(): string` → "Доброе утро" / "Добрый день" / "Добрый вечер"

Создать `src/lib/constants.ts` с константами:
- `CONSULTATION_STATUSES` — все статусы с цветами и лейблами
- `EMPLOYEE_ROLES` — роли с лейблами
- `ATTENDANCE_STATUSES` — статусы явки

**Какие файлы изменяются:**
- `src/lib/formatters.ts` — создание нового файла
- `src/lib/constants.ts` — создание нового файла

**Supabase таблицы:** нет

**Зависимости:** нет

**Критерии готовности:**
- [ ] Все функции форматирования работают корректно
- [ ] `formatMoney(24500)` → `"24 500 KGS"`
- [ ] `formatPercent(36.4567)` → `"36.5%"`
- [ ] Константы статусов содержат цвета из Design System (не произвольные)

---

## Фаза 1: Layout

### Задача 1.1 — Редизайн Sidebar

**Что будет сделано:**
Полностью переписать `src/components/Sidebar.tsx` по Design System:
- Фон: `#0c1f33` (brand dark navy, не gray-950)
- Логотип: прямоугольная марка с текстом "DR" на `#0c4d6c` фоне
- Навигационные группы с обновлёнными ролями (`owner/rop/mp/lmai/accountant`)
- Активная ссылка: `#0c4d6c` фон, белый текст, без фиолетового
- Hover: `rgba(255,255,255,0.06)` фон
- Иконки: 16px, `#a2b4c0` цвет (steel), при активном — белый
- Группы: заголовки 10px uppercase `#a2b4c0` с отступом
- Пользовательский блок внизу: аватар + имя + роль + кнопка выхода
- Ширина: 192px (убрать лишние 8px от текущих 240px)
- Убрать CommandPalette search hint из sidebar (перенести в Topbar)

Обновить роли в навигации:
```
Главное:     Dashboard, Записи
Аналитика:   Декомпозиция, Зарплаты (owner/rop/accountant), Финансы (owner/accountant)
Управление:  Сотрудники (owner/rop), Документы (owner), Настройки (owner)
Интеграции:  Интеграции (owner)
```

**Какие файлы изменяются:**
- `src/components/Sidebar.tsx` — полная замена содержимого
- `src/config/nav.ts` — создать (конфигурация навигации вынесена)

**Supabase таблицы:** нет (данные пользователя из AuthContext)

**Зависимости:** нет (Sidebar не зависит от типов Dashboard)

**Критерии готовности:**
- [ ] Цвет sidebar `#0c1f33`
- [ ] Ширина 192px
- [ ] Активный пункт подсвечен `#0c4d6c`
- [ ] Роли `owner/rop/mp/lmai/accountant` — каждая видит правильные пункты
- [ ] Нет фиолетовых цветов
- [ ] Пользовательский блок внизу корректно отображает имя и роль
- [ ] Sidebar не ломает другие существующие страницы

---

### Задача 1.2 — Топбар

**Что будет сделано:**
Создать `src/components/layout/Topbar.tsx`:
- Высота: 52px, `backdrop-filter: blur(12px)`
- Фон: `rgba(255,255,255,0.85)` (белое стекло)
- Нижняя граница: `1px solid rgba(0,0,0,0.06)`
- Слева: название текущей страницы (breadcrumb)
- По центру: поисковый хинт `⌘K — быстрый поиск` (перенесён из Sidebar)
- Справа: дата (`Пятница, 27 июня`) + индикатор онлайн + колокол уведомлений (badge) + аватар
- `sticky top-0 z-40`

**Какие файлы изменяются:**
- `src/components/layout/Topbar.tsx` — создание
- `src/app/dashboard/layout.tsx` — добавить Topbar в layout

**Supabase таблицы:**
- `notifications` — COUNT непрочитанных для badge (простой запрос)

**Зависимости:** Задача 1.1 (Sidebar должен быть 192px чтобы Topbar правильно начинался)

**Критерии готовности:**
- [ ] Topbar sticky, не перекрывает контент
- [ ] Backdrop blur работает
- [ ] Дата отображается на русском
- [ ] Badge на колоколе показывает количество (0 — не показывает)
- [ ] Аватар пользователя (инициалы если нет фото)
- [ ] Нет сдвига layout при sticky позиционировании

---

### Задача 1.3 — Dashboard Layout (Split Focus структура)

**Что будет сделано:**
Переписать `src/app/dashboard/page.tsx` — только структура (без данных):
- Убрать весь старый код (фиолетовый дизайн)
- Создать двухпанельный layout:
  ```
  ┌─────────────────┬─────────────────┐
  │   LEFT PANEL    │   RIGHT PANEL   │
  │   (white bg)    │   (#f5f6f8 bg)  │
  │   month data    │   today data    │
  │   ~55% width    │   ~45% width    │
  └─────────────────┴─────────────────┘
  ```
- Левая панель: `bg-white`, padding `p-6`, `border-r border-[#e5e7eb]`
- Правая панель: `bg-[#f5f6f8]`, padding `p-6`
- Обе панели: `overflow-y-auto`, высота `calc(100vh - 52px)` (минус topbar)
- Заглушки для каждого блока (skeleton-подобные div'ы с `rounded-xl bg-gray-100`)

Заглушки левой панели:
1. Заголовок "Текущий месяц" + дата
2. Блок KPI карточек (4 карточки в сетке 2x2)
3. Блок "Выручка по неделям" (мини бар-чарт)
4. Блок "План vs Факт" (таблица)

Заглушки правой панели:
1. Заголовок "Сегодня" + дата
2. Блок "Сегодня" (2 карточки)
3. Блок "Живая лента"
4. Блок "Команда сейчас"
5. Блок "Расписание"

**Какие файлы изменяются:**
- `src/app/dashboard/page.tsx` — полная замена (только структура)

**Supabase таблицы:** нет (только layout)

**Зависимости:** Задача 1.1, 1.2

**Критерии готовности:**
- [ ] Двухпанельный layout отображается корректно
- [ ] Обе панели прокручиваются независимо
- [ ] `overflow-x-hidden` на уровне `<main>`
- [ ] На экранах < 1280px: панели складываются вертикально (left наверх, right вниз)
- [ ] Нет горизонтальной прокрутки
- [ ] Старый код полностью удалён

---

## Фаза 2: Левая панель — Данные за месяц

### Задача 2.1 — Функции получения данных за месяц

**Что будет сделано:**
Создать `src/lib/dashboard-queries.ts` — чистые функции для запросов к Supabase.
Все функции — async, принимают Supabase client, возвращают типизированные данные.

Функции для левой панели:
```typescript
getMonthKpiStats(supabase, year, month, employeeId?)
  → { fv: number, sales: number, revenue: number, kpiPct: number }

getMonthPlanStats(supabase, year, month, employeeId?)
  → { planFv: number, planSales: number, planRevenue: number }

getRevenueByWeeks(supabase, year, month)
  → Array<{ week: string, revenue: number }>

getPlanVsFactTable(supabase, year, month)
  → Array<{ employeeId, name, planFv, factFv, planSales, factSales, planRevenue, factRevenue, kpiPct }>
```

Каждая функция:
- Обрабатывает `error` от Supabase (бросает или возвращает null-safe значения)
- Адаптирована к **реальной схеме БД** (определённой в задаче 0.1)

**Какие файлы изменяются:**
- `src/lib/dashboard-queries.ts` — создание

**Supabase таблицы:**
- `consultations` (fv=count пришедших, sales=count купивших, revenue=sum amount)
- `daily_facts` (если существует — используем для override)
- `decomposition` (если существует — агрегат)
- `employee_kpi` (планы)
- `employees` (список с именами)

**Зависимости:** Задача 0.1 (знаем реальную схему), 0.2 (типы)

**Критерии готовности:**
- [ ] Все функции типизированы, нет `any`
- [ ] Ошибки Supabase обрабатываются
- [ ] Функции работают при пустой БД (возвращают нули, не падают)
- [ ] Каждая функция — отдельный `await`, можно вызывать через `Promise.all`

---

### Задача 2.2 — KPI карточки месяца (левая панель)

**Что будет сделано:**
Создать `src/components/dashboard/MonthKpiCards.tsx`:
- 4 карточки в сетке `2x2`: ФВ, Продажи, Выручка, KPI%
- Дизайн по Design System: нет рамок, только `box-shadow`, белый фон
- Каждая карточка:
  - Иконка (16px, `#0c4d6c`)
  - Лейбл (`11px, #6b7280`)
  - Значение (`28px, font-bold, tabular-nums`)
  - Прогресс: тонкая `2px` полоска (план/факт)
  - Под значением: процент выполнения `+N от плана`

Создать переиспользуемый `src/components/common/KpiCard.tsx`:
```
Props: { label, value, icon, progress?, suffix?, color? }
Цвет прогресса:
  0–30%:   #dc2626 (danger)
  30–70%:  #d97706 (warning)
  70–100%: #0c4d6c (brand)
  100%+:   #16a34a (success)
```

Skeleton состояние (пока данные загружаются):
- `src/components/common/KpiCardSkeleton.tsx`
- Shimmer анимация по Design System

**Какие файлы изменяются:**
- `src/components/dashboard/MonthKpiCards.tsx` — создание
- `src/components/common/KpiCard.tsx` — создание
- `src/components/common/KpiCardSkeleton.tsx` — создание

**Supabase таблицы:**
- Данные через функции из задачи 2.1 (не прямые запросы)

**Зависимости:** Задача 2.1, Задача 0.3 (форматтеры)

**Критерии готовности:**
- [ ] 4 карточки отображаются в сетке 2x2
- [ ] Прогресс-бар меняет цвет по порогам
- [ ] Значения форматированы: деньги через `formatMoney`, проценты через `formatPercent`
- [ ] Skeleton корректно занимает то же место что и реальный контент
- [ ] При `value = 0` карточка не ломается (показывает "0", прогресс 0%)
- [ ] Нет фиолетовых цветов

---

### Задача 2.3 — Мини бар-чарт "Выручка по неделям"

**Что будет сделано:**
Создать `src/components/dashboard/RevenueWeeksChart.tsx`:
- Заголовок: "Выручка" + текущий месяц + итоговая сумма месяца
- Горизонтальный бар-чарт или вертикальный: 4–5 баров (неделя 1, 2, 3, 4)
- Каждый бар: `#0c4d6c` цвет, `border-radius: 4px`, высота пропорциональна выручке
- Hover: tooltip с суммой недели (`formatMoney`)
- Внизу: `Нд 1`, `Нд 2`, `Нд 3`, `Нд 4` подписи
- Если нет данных: EmptyState "Нет продаж в этом месяце"

Использовать Recharts (`BarChart`, `Bar`, `Tooltip`) согласно Design System.

Skeleton: 4 прямоугольника разной высоты с shimmer.

**Какие файлы изменяются:**
- `src/components/dashboard/RevenueWeeksChart.tsx` — создание

**Supabase таблицы:**
- `consultations` (sum amount по датам, группировка по неделям через JS)

**Зависимости:** Задача 2.1, 0.3

**Критерии готовности:**
- [ ] Chart рендерится без ошибок
- [ ] Данные группируются корректно по ISO неделям
- [ ] Tooltip показывает отформатированную сумму
- [ ] `ResponsiveContainer` — chart заполняет доступную ширину
- [ ] Нет ошибок в консоли при пустых данных

---

### Задача 2.4 — Таблица "План vs Факт" (левая панель)

**Что будет сделано:**
Создать `src/components/dashboard/PlanVsFactTable.tsx`:
- Заголовок: "Команда за месяц"
- Колонки: Сотрудник | ФВ (план/факт) | Продажи (план/факт) | Выручка | KPI%
- Строки: по одной на каждого сотрудника команды
- Дизайн таблицы по Design System:
  - Нет внешних рамок у таблицы
  - `border-bottom: 1px solid #f3f4f6` на строках
  - Hover строки: `rgba(0,0,0,.02)` фон
  - Числа: `tabular-nums`, right-aligned
  - Пустые значения: "—"
- KPI% колонка: цветной badge (`danger/warning/brand/success` по порогам)
- Аватар сотрудника (инициалы) + имя в первой колонке

Для РОП: видит всю команду.
Для МП/LMAI: видит только себя (одна строка).
Для Владельца: видит всех.

Skeleton: 3 строки-заглушки с shimmer.

**Какие файлы изменяются:**
- `src/components/dashboard/PlanVsFactTable.tsx` — создание

**Supabase таблицы:**
- `employees` (список)
- `consultations` или `decomposition` (агрегат через dashboard-queries.ts)
- `employee_kpi` (планы)

**Зависимости:** Задача 2.1, 0.2, 0.3

**Критерии готовности:**
- [ ] Таблица отображает данные для всех ролей корректно
- [ ] РОП видит только свою команду
- [ ] МП видит только себя
- [ ] Числа tabular-nums, выровнены по правому краю
- [ ] Пустые значения показывают "—" (em-dash)
- [ ] При 0 сотрудниках: EmptyState

---

### Задача 2.5 — Сборка Left Panel

**Что будет сделано:**
Создать `src/components/dashboard/LeftPanel.tsx`:
- Server Component
- Получает данные за текущий месяц через `dashboard-queries.ts`
- Отображает все блоки левой панели:
  1. Заголовок "Текущий месяц · {Июнь 2025}"
  2. `<MonthKpiCards />`
  3. `<RevenueWeeksChart />`
  4. `<PlanVsFactTable />`
- Передаёт данные в Client-компоненты через props (не fetch внутри каждого)
- Обёрнут в `<Suspense fallback={<LeftPanelSkeleton />}>`

Создать `src/components/dashboard/LeftPanelSkeleton.tsx`:
- Заглушка всей левой панели (4 карточки + чарт + таблица)

**Какие файлы изменяются:**
- `src/components/dashboard/LeftPanel.tsx` — создание
- `src/components/dashboard/LeftPanelSkeleton.tsx` — создание
- `src/app/dashboard/page.tsx` — подключить `<LeftPanel />`

**Supabase таблицы:** через `dashboard-queries.ts`

**Зависимости:** Задачи 2.2, 2.3, 2.4

**Критерии готовности:**
- [ ] Левая панель полностью отображает данные за месяц
- [ ] Suspense работает — skeleton виден при загрузке
- [ ] Нет waterfall запросов (все запросы через `Promise.all`)
- [ ] Ошибка одного блока не роняет всю панель
- [ ] Данные корректны для всех ролей

---

## Фаза 3: Правая панель — Данные за сегодня

### Задача 3.1 — Функции получения данных за сегодня

**Что будет сделано:**
Дополнить `src/lib/dashboard-queries.ts` функциями для правой панели:

```typescript
getTodayStats(supabase, date, employeeId?)
  → { fvToday: number, salesToday: number, revenueToday: number }

getLiveFeed(supabase, date, limit = 20)
  → Array<LiveFeedItem> // последние события за сегодня

getTeamNow(supabase, date)
  → Array<{ employee, attendance, lastActivity }>

getTodaySchedule(supabase, date)
  → Array<{ time, clientName, managerName, status, format }>
```

**Какие файлы изменяются:**
- `src/lib/dashboard-queries.ts` — добавление функций

**Supabase таблицы:**
- `consultations` (сегодняшние записи)
- `attendance` (кто вышел)
- `employees` (список команды)
- `schedules` (расписание если есть)

**Зависимости:** Задача 0.1, 0.2

**Критерии готовности:**
- [ ] Все функции возвращают пустые массивы при отсутствии данных (не null)
- [ ] `getLiveFeed` отсортирован по времени DESC
- [ ] `getTeamNow` показывает статус присутствия
- [ ] Нет N+1 запросов

---

### Задача 3.2 — Карточки "Сегодня"

**Что будет сделано:**
Создать `src/components/dashboard/TodayCards.tsx`:
- 2 карточки рядом: "ФВ сегодня" и "Продажи сегодня"
- Дизайн: чуть компактнее KPI карточек месяца (меньше padding)
- ФВ карточка: число + "из {план} запланировано"
- Продажи: число + сумма в KGS
- Цвет фона: чуть серее (правая панель `#f5f6f8` — карточки `white`)

**Какие файлы изменяются:**
- `src/components/dashboard/TodayCards.tsx` — создание

**Supabase таблицы:** через функции задачи 3.1

**Зависимости:** Задача 3.1, 0.3

**Критерии готовности:**
- [ ] Карточки отображаются корректно при 0 значениях
- [ ] Числа tabular-nums
- [ ] Форматирование: деньги через `formatMoney`

---

### Задача 3.3 — Живая лента событий

**Что будет сделано:**
Создать `src/components/dashboard/LiveFeed.tsx` (Client Component):
- Заголовок: "Сегодня" + количество записей
- Список событий: каждая строка — консультация за сегодня
- Каждый элемент ленты:
  - Цветной индикатор статуса (левая полоска 3px)
  - Время (`formatTime`)
  - Имя клиента (жирный)
  - Статус badge (`formatStatus()` → цвет из `CONSULTATION_STATUSES`)
  - Менеджер (серый, маленький)
  - Сумма (если есть продажа, зелёный)
- При `actual_status = 'Пришла'` → зелёный accent
- При `status_after_fv = 'Купила'` → бирюзовый accent + сумма
- При `status_after_fv = 'Отказ'` → красный accent

Пустое состояние: "Записей на сегодня нет" с иконкой Calendar.

**Какие файлы изменяются:**
- `src/components/dashboard/LiveFeed.tsx` — создание

**Supabase таблицы:** данные через props от RightPanel

**Зависимости:** Задача 3.1, 0.3

**Критерии готовности:**
- [ ] Лента сортирована по времени (ближайшие сверху)
- [ ] Цвета соответствуют Design System (не произвольные)
- [ ] EmptyState при отсутствии записей
- [ ] Прокрутка внутри ленты если записей много (max-height + overflow-y)
- [ ] Нет горизонтального переполнения при длинных именах

---

### Задача 3.4 — Блок "Команда сейчас"

**Что будет сделано:**
Создать `src/components/dashboard/TeamNow.tsx`:
- Заголовок: "Команда" + "{N} онлайн"
- Список сотрудников с их статусом на сегодня:
  - Аватар (инициалы + цвет фона по роли)
  - Имя + роль
  - Статус присутствия: зелёная точка (работает) / серая (выходной) / жёлтая (больничный)
  - Последняя активность (когда последний раз была запись)
- Компактный дизайн: каждая строка 40px высота

Видимость по ролям:
- Владелец: вся команда
- РОП: только свой отдел
- МП/LMAI: только себя

**Какие файлы изменяются:**
- `src/components/dashboard/TeamNow.tsx` — создание

**Supabase таблицы:** данные через props от RightPanel

**Зависимости:** Задача 3.1

**Критерии готовности:**
- [ ] Статус присутствия отображается корректно
- [ ] "N онлайн" считается только для `attendance.status = 'worked'`
- [ ] Цветовые точки: зелёная/серая/жёлтая/красная по статусу
- [ ] Пустое состояние при 0 сотрудниках

---

### Задача 3.5 — Блок "Расписание на сегодня"

**Что будет сделано:**
Создать `src/components/dashboard/TodaySchedule.tsx`:
- Заголовок: "Расписание" + текущее время
- Список консультаций на сегодня, упорядоченных по времени
- Каждая запись:
  - Время (жирное, `tabular-nums`)
  - Имя клиента
  - Формат (Онлайн/Офлайн badge)
  - Менеджер (если владелец/РОП)
  - Текущий статус
- Текущее время: "сейчас" стрелка между прошедшими и будущими
- Прошедшие: `opacity-50`
- Предстоящие: полная непрозрачность

**Какие файлы изменяются:**
- `src/components/dashboard/TodaySchedule.tsx` — создание

**Supabase таблицы:** данные через props от RightPanel

**Зависимости:** Задача 3.1, 0.3

**Критерии готовности:**
- [ ] Расписание отсортировано по времени ASC
- [ ] "Сейчас" разделитель находится в правильном месте
- [ ] Прошедшие записи визуально приглушены
- [ ] Пустое состояние: "Нет записей на сегодня"

---

### Задача 3.6 — Сборка Right Panel

**Что будет сделано:**
Создать `src/components/dashboard/RightPanel.tsx`:
- Server Component
- Получает данные за сегодня через `dashboard-queries.ts`
- Отображает все блоки правой панели:
  1. Заголовок "Сегодня · {Пятница, 27 июня}"
  2. `<TodayCards />`
  3. `<LiveFeed />`
  4. `<TeamNow />`
  5. `<TodaySchedule />`
- `<Suspense fallback={<RightPanelSkeleton />}>`

Создать `src/components/dashboard/RightPanelSkeleton.tsx`.

Обновить `src/app/dashboard/page.tsx` — подключить обе панели.

**Какие файлы изменяются:**
- `src/components/dashboard/RightPanel.tsx` — создание
- `src/components/dashboard/RightPanelSkeleton.tsx` — создание
- `src/app/dashboard/page.tsx` — финальная сборка

**Supabase таблицы:** через `dashboard-queries.ts`

**Зависимости:** Задачи 3.2, 3.3, 3.4, 3.5

**Критерии готовности:**
- [ ] Правая панель полностью отображает данные за сегодня
- [ ] Suspense работает
- [ ] Данные актуальны (запрашиваются при каждом открытии страницы)
- [ ] Dashboard полностью собран: Left + Right панели работают вместе

---

## Фаза 4: Realtime и Polish

### Задача 4.1 — Supabase Realtime для Live Feed

**Что будет сделано:**
Добавить real-time обновления в `LiveFeed.tsx`:
- Подписка на `consultations` INSERT/UPDATE для сегодняшней даты
- При новом событии: новая строка появляется вверху ленты с анимацией (fade + slide down)
- При обновлении статуса: строка обновляется плавно
- Cleanup: отписка в `useEffect` return

Анимация появления новой записи:
```css
@keyframes feedIn {
  from { opacity: 0; transform: translateY(-8px); }
  to   { opacity: 1; transform: translateY(0); }
}
```

**Какие файлы изменяются:**
- `src/components/dashboard/LiveFeed.tsx` — добавить Realtime subscription

**Supabase таблицы:**
- `consultations` — Realtime channel (FILTER: date=today)

**Зависимости:** Задача 3.3

**Критерии готовности:**
- [ ] Новая запись появляется без перезагрузки страницы
- [ ] Анимация плавная (не скачок)
- [ ] Channel отписывается при unmount компонента
- [ ] Нет дублирования записей при reconnect
- [ ] Работает при нескольких открытых вкладках

---

### Задача 4.2 — Ролевая фильтрация данных

**Что будет сделано:**
Убедиться что все блоки Dashboard показывают правильные данные для каждой роли.
Проверить и исправить если нужно:

| Блок | owner | rop | mp | lmai | accountant |
|------|-------|-----|-----|------|-----------|
| Month KPI | Всей компании | Своего отдела | Только свои | Только свои | — (redirect) |
| Revenue chart | Вся компания | Свой отдел | Свои | — | — |
| Plan vs Fact | Все | Команда | Только себя | Только себя | — |
| Today cards | Компания | Отдел | Свои | Свои | — |
| Live Feed | Все | Отдел | Свои | Свои | — |
| Team Now | Все | Отдел | Только себя | Только себя | — |
| Schedule | Все | Отдел | Свои | Свои | — |

Для бухгалтера: Dashboard перенаправляет на `/dashboard/finances`.

**Какие файлы изменяются:**
- `src/lib/dashboard-queries.ts` — добавить `employeeId` и `role` фильтрацию
- `src/components/dashboard/LeftPanel.tsx` — передать роль/id
- `src/components/dashboard/RightPanel.tsx` — передать роль/id
- `src/app/dashboard/page.tsx` — читать роль из сессии, передавать в панели

**Supabase таблицы:** RLS уже фильтрует, но нужна дополнительная логика в запросах

**Зависимости:** Задачи 2.5, 3.6

**Критерии готовности:**
- [ ] Войти как МП — видит только свои данные
- [ ] Войти как РОП — видит данные своего отдела
- [ ] Войти как Владелец — видит все
- [ ] Бухгалтер → redirect на /dashboard/finances
- [ ] Нет SQL ошибок при любой роли

---

### Задача 4.3 — Loading, Empty и Error состояния

**Что будет сделано:**
Финальная проверка и доработка всех состояний:

**Loading (Skeleton):**
- Каждый блок имеет skeleton при загрузке
- Skeleton использует shimmer анимацию из Design System
- Размер skeleton совпадает с размером реального контента

**Empty States:**
- KPI карточки при `value = 0`: показывают 0, не скрываются
- PlanVsFact при 0 сотрудниках: "Нет данных за этот месяц" + кнопка "Добавить сотрудника"
- LiveFeed без записей: "Записей на сегодня нет" с иконкой
- TeamNow без сотрудников: "Команда не настроена"
- TodaySchedule без записей: "Нет записей на сегодня"

**Error States:**
- При ошибке запроса: красная плашка в блоке с "Попробовать снова"
- Ошибка одного блока не роняет другие (Error Boundary на каждый блок)

Создать:
- `src/components/common/BlockError.tsx`
- `src/components/common/DashboardErrorBoundary.tsx`

**Какие файлы изменяются:**
- `src/components/common/BlockError.tsx` — создание
- `src/components/common/DashboardErrorBoundary.tsx` — создание
- Все компоненты Dashboard — добавить обёртку

**Supabase таблицы:** нет

**Зависимости:** Задача 2.5, 3.6

**Критерии готовности:**
- [ ] Каждый блок имеет все 3 состояния: loading / empty / data
- [ ] Error boundary ловит ошибки рендера
- [ ] "Попробовать снова" выполняет refetch
- [ ] При сетевой ошибке: понятное сообщение на русском

---

### Задача 4.4 — Финальный UI polish

**Что будет сделано:**
Финальная проверка всего Dashboard на соответствие Design System:

**Цвета:**
- [ ] Нет фиолетовых (`#8b5cf6`, `violet-*`) — только brand palette
- [ ] Sidebar: `#0c1f33`
- [ ] Brand accent: `#0c4d6c`
- [ ] Нейтральные: `#f5f6f8`, `#e5e7eb`, `#6b7280`

**Типографика:**
- [ ] Все числа: `font-feature-settings: 'tnum'` (tabular-nums)
- [ ] Заголовки блоков: 14px medium
- [ ] Подзаголовки: 11px normal #6b7280

**Карточки:**
- [ ] Нет видимых border (только `box-shadow`)
- [ ] Правильный shadow: `0 1px 3px rgba(0,0,0,.04), 0 0 0 1px rgba(0,0,0,.03)`
- [ ] `border-radius: 12px` на карточках

**Анимации:**
- [ ] Hover карточек: `translateY(-1px)` + shadow ramp (150ms)
- [ ] Нет резких переходов

**Прочее:**
- [ ] Нет console.log в production коде
- [ ] Нет закомментированного кода
- [ ] `tsc --noEmit` — без ошибок
- [ ] `next lint` — без ошибок

**Какие файлы изменяются:**
- Все компоненты Dashboard по результатам проверки

**Supabase таблицы:** нет

**Зависимости:** Все предыдущие задачи

**Критерии готовности:**
- [ ] Визуально совпадает с утверждённым Premium Split Focus дизайном
- [ ] Нет фиолетовых элементов нигде
- [ ] Все чеклисты из Design System (06_UI_Design_System.md) выполнены

---

## Сводная таблица задач

| # | Задача | Фаза | Часы | Зависимости | Файлов |
|---|--------|------|------|------------|--------|
| 0.1 | Ревизия БД | Foundation | 1ч | — | 0 |
| 0.2 | TypeScript типы | Foundation | 1ч | 0.1 | 1 |
| 0.3 | Форматтеры и константы | Foundation | 1ч | — | 2 |
| 1.1 | Редизайн Sidebar | Layout | 2ч | — | 2 |
| 1.2 | Топбар | Layout | 1.5ч | 1.1 | 2 |
| 1.3 | Dashboard Layout структура | Layout | 1ч | 1.1, 1.2 | 1 |
| 2.1 | Функции данных за месяц | Data | 1.5ч | 0.1, 0.2 | 1 |
| 2.2 | KPI карточки месяца | Components | 2ч | 2.1, 0.3 | 3 |
| 2.3 | Revenue by weeks chart | Components | 1.5ч | 2.1, 0.3 | 1 |
| 2.4 | Plan vs Fact таблица | Components | 2ч | 2.1, 0.2, 0.3 | 1 |
| 2.5 | Сборка Left Panel | Assembly | 1ч | 2.2, 2.3, 2.4 | 3 |
| 3.1 | Функции данных за сегодня | Data | 1.5ч | 0.1, 0.2 | 1 |
| 3.2 | Карточки Сегодня | Components | 1ч | 3.1, 0.3 | 1 |
| 3.3 | Живая лента | Components | 1.5ч | 3.1, 0.3 | 1 |
| 3.4 | Команда сейчас | Components | 1ч | 3.1 | 1 |
| 3.5 | Расписание на сегодня | Components | 1ч | 3.1, 0.3 | 1 |
| 3.6 | Сборка Right Panel | Assembly | 1ч | 3.2–3.5 | 3 |
| 4.1 | Realtime для Live Feed | Polish | 1.5ч | 3.3 | 1 |
| 4.2 | Ролевая фильтрация | Polish | 2ч | 2.5, 3.6 | 4 |
| 4.3 | Loading/Empty/Error | Polish | 2ч | 2.5, 3.6 | 2+ |
| 4.4 | UI Polish | Polish | 1.5ч | Все | Все |

**Итого:** 21 задача · ~31 час разработки

---

## Порядок выполнения (рекомендуемый)

```
Неделя 1: Foundation + Layout
  Пн: 0.1 + 0.2 + 0.3
  Вт: 1.1 + 1.2
  Ср: 1.3 + 2.1 + 3.1
  Чт: 2.2 + 2.3
  Пт: 2.4 + 2.5

Неделя 2: Right Panel + Polish
  Пн: 3.2 + 3.3 + 3.4
  Вт: 3.5 + 3.6
  Ср: 4.1 + 4.2
  Чт: 4.3
  Пт: 4.4 + финальная проверка
```

---

## Definition of Done для Dashboard модуля

Dashboard считается готовым когда:

```
□ Все 21 задачи выполнены
□ Split Focus дизайн реализован (Left: месяц / Right: сегодня)
□ Все 5 ролей видят правильные данные
□ Sidebar цвет #0c1f33, без фиолетового
□ Realtime обновления в Live Feed работают
□ Dashboard работает при пустой БД (нет ошибок)
□ Dashboard работает при наполненной БД
□ tsc --noEmit — без ошибок
□ next lint — без ошибок
□ Нет console.log в коде
□ Нет фиолетовых (#8b5cf6) элементов
□ Все числа tabular-nums
□ Loading skeleton — есть
□ Empty state — есть
□ Error state — есть
□ Страница загружается < 3 секунды (локально)
```

---

*Документ готов к подтверждению. После подтверждения — начинаем с задачи 0.1.*
