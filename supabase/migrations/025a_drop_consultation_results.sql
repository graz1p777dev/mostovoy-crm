-- Migration 025a: удаление таблицы consultation_results
--
-- Таблица создана в 007, обвешана RLS-политиками в 025 и удалена как
-- неиспользуемая. В облачной базе удаление сделали вручную, мимо миграций
-- (см. PROJECT_STATE.md, раздел «Переименования и удаления»).
--
-- Идемпотентна: на боевой базе таблицы уже нет, DROP ... IF EXISTS ничего не делает.
-- CASCADE снимает вместе с таблицей её RLS-политики из 025.

DROP TABLE IF EXISTS public.consultation_results CASCADE;
